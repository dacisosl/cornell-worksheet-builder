{
  "name": "cornell-worksheet-builder",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
